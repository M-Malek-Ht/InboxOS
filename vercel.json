{
  "version": 2,
  "installCommand": "npm install && cd inboxos-backend && npm install --include=dev && cd ../inbox-flow-main && npm install --include=dev",
  "buildCommand": "cd inboxos-backend && npm run build && cd ../inbox-flow-main && npm run build && cd .. && cp -r inboxos-backend/dist api/backend-dist && cp -r inboxos-backend/node_modules api/backend-dist/node_modules",
  "outputDirectory": "inbox-flow-main/dist",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
