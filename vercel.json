{
  "version": 2,
  "installCommand": "npm install && cd inboxos-backend && npm install --include=dev && cd ../inbox-flow-main && npm install --include=dev",
  "buildCommand": "cd inboxos-backend && npm run build && cd ../inbox-flow-main && npm run build",
  "outputDirectory": "inbox-flow-main/dist",
  "functions": {
    "api/index.js": {
      "includeFiles": ["inboxos-backend/dist/**", "inboxos-backend/node_modules/**"],
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
